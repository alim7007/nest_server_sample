- var appLogo = process.env.APPLOGO || ''
- var appName = process.env.APPNAME || ''

p Dear #{fullName},

p You or someone else requested OTP code to reset password!

p Please enter this code in the website to set your new password:
p <strong>#{otpCode}</strong>
               
p If you didn't request reset password OTP code, please disregard this message.

div(style="display:flex;")
 img(src=appLogo  alt=appName width="88" height="88" style="margin: 0; border: 0; padding: 0; vertical-align:middle;")
 span &copy; #{(new Date()).getFullYear()} 
 | The #{appName} #{supportPhone? `Support Team, phone: ${supportPhone}`:'Support Team'}

